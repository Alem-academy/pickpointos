# Инструкция по развертыванию на Railway 🚂

Это руководство поможет вам развернуть проект **PVZ Management OS** на платформе Railway.

## Шаг 1: Подготовка
Убедитесь, что ваш код загружен в репозиторий GitHub.

## Шаг 2: Создание проекта в Railway
1. Зайдите на [railway.app](https://railway.app/) и авторизуйтесь через GitHub.
2. Нажмите **"New Project"** -> **"Empty Project"**.

## Шаг 3: База данных и Redis
1. В вашем новом проекте нажмите ПКМ (или кнопку "Create") -> **Database**.
2. Выберите **PostgreSQL**.
3. Еще раз нажмите ПКМ -> **Database** -> **Redis**.
4. Дождитесь, пока обе иконки станут зелеными (Active).

## Шаг 4: Развертывание Бэкенда
1. Нажмите **"Create"** -> **"GitHub Repo"**.
2. Выберите ваш репозиторий `AlemLab-pickpointoperations`.
3. Нажмите на появившуюся карточку сервиса, перейдите в **Settings**.
4. Найдите раздел **Root Directory** и укажите: `/parser`.
5. Перейдите во вкладку **Variables** и добавьте переменные:
   - `PORT`: `8080`
   - `DATABASE_URL`: *Нажмите Reference Variable и выберите `DATABASE_URL` от Postgres*
   - `REDIS_URL`: *Нажмите Reference Variable и выберите `REDIS_URL` от Redis*
   - `CORS_ORIGIN`: `*` (или URL фронтенда, который мы получим позже)
6. Сервис автоматически пересоберется (Deploy).

## Шаг 5: Инициализация Базы Данных
1. В Railway нажмите на карточку **PostgreSQL**.
2. Перейдите во вкладку **Data**.
3. Скопируйте содержимое файла `database/schema.sql` из вашего проекта.
4. Вставьте его в поле SQL Query в Railway (или используйте внешний клиент, подключившись по данным из вкладки Connect) и нажмите Run.

## Шаг 6: Развертывание Фронтенда
1. Нажмите **"Create"** -> **"GitHub Repo"**.
2. Снова выберите тот же репозиторий `AlemLab-pickpointoperations`.
3. Нажмите на новую карточку сервиса, перейдите в **Settings**.
4. **Root Directory** оставьте пустым (корневая папка).
5. Проверьте, что Railway определил Dockerfile (обычно это происходит автоматически).
6. Нажмите **"Generate Domain"** (или **Networking** -> **Public Networking**), чтобы получить публичную ссылку на ваш сайт.

## Шаг 7: Соединение
1. Скопируйте домен, который Railway выдал вашему **Бэкенду** (например, `web-production-1234.up.railway.app`).
2. В коде фронтенда убедитесь, что API запросы идут на этот адрес.
   - *Совет*: Лучше всего вынести API_URL в переменную окружения фронтенда `VITE_API_URL` и добавить ее в Variables сервиса Frontend в Railway.
   - Если вы хардкодили `localhost` в коде, вам нужно будет заменить его на `https://ваш-бэкенд.railway.app`.

🎉 Готово! Ваше приложение должно быть доступно по ссылке фронтенда.
