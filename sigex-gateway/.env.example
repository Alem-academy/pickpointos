PORT=8080
SIGEX_API_URL=https://sigex.kz/api
# Add any API keys or secrets for SIGEX if required

# Облегчённый HTML→PDF для PickPoint (POST /api/render/html-to-pdf). Должен совпадать с PDF_RENDER_GATEWAY_SECRET на Railway.
PDF_RENDER_GATEWAY_SECRET=
# Опционально: системный Chromium (dnf install chromium)
# PUPPETEER_EXECUTABLE_PATH=/usr/bin/chromium-browser
