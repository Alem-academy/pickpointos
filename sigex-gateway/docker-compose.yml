services:
  sigex-gateway:
    build: .
    container_name: sigex-gateway
    ports:
      - "8080:8080"
    env_file:
      - .env
    restart: always
    deploy:
      resources:
        limits:
          cpus: '0.8'
          memory: 400M
